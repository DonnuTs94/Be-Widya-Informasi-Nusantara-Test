const LIMIT_FILE_SIZE = "LIMIT_FILE_SIZE"
const SIZE_3MB = 3 * 1024 * 1024
const PATH = "public"
const FILE_TYPES = ["image/jpg", "image/jpeg", "image/png"]
const FILE_PREFIX = "product_url"

export { LIMIT_FILE_SIZE, SIZE_3MB, PATH, FILE_TYPES, FILE_PREFIX }
